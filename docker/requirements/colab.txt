# AUTO-GENERATED placeholder — regenerate with scripts/compile_requirements.ps1.
# Installed on top of Colab's preinstalled, CUDA-matched torch — deliberately
# does NOT pin torch itself (installing a different torch build would break
# Colab's CUDA setup).
transformers
peft
accelerate
bitsandbytes
datasets
safetensors
numpy
pandas
matplotlib
seaborn
pydantic
pyyaml
